# Upstart script for thalassa-server
respawn

setuid <%= node[:thalassa][:user] %>
setgid <%= node[:thalassa][:group] %>

start on filesystem
stop on runlevel [06]

exec <%= node[:thalassa][:install_dir] %>/node_modules/.bin/thalassa-server
